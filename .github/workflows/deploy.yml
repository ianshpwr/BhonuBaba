name: Legacy Deploy Notice

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest

    steps:
      - name: Explain deployment workflow
        run: echo "Deployment is now handled by .github/workflows/ci.yml using Terraform, ECR, and ECS Fargate."
